from abc import staticmethod
class GameMapperController:
    # el proceso inverso podría ir en el servicio porque se construirían los datos que no pasa el cliente

    @staticmethod
    def domain_model_to_controller_model(domain_model):
        pass